import {type Appointment, Pet, User} from "../types/SystemTypes.ts";

interface Props {
    appointment: Appointment,
    user: User,
    onReserve: (appointmentId: number) => void;
}

function ReserveAppointmentCard(props: Props) {
    // Variables
    // -----------------------------------------------------------------------------------------------------------------
    
    // Functions
    // -----------------------------------------------------------------------------------------------------------------

    // Return
    // -----------------------------------------------------------------------------------------------------------------

    return
}

export default ReserveAppointmentCard;